version: "v1"
status: "FROZEN"
generated_on: "2026-02-17"

phases:
  pilot:
    description: "Short iso-step screening; early-stop practically disabled."
    max_steps: 12000
    val_check_every_steps: 2000
    patience_evals: 9999
    min_delta: 0.0
    min_steps_before_early_stop: 12001

  final:
    description: "Full-budget confirmatory training."
    max_steps: 60000
    val_check_every_steps: 1000
    patience_evals: 10
    min_delta: 0.0
    min_steps_before_early_stop: 12000

notes:
  - "Use pilot to shortlist; run final only on shortlisted candidates."
  - "For final phase, if reference model has not plateaued, increase max_steps globally."
